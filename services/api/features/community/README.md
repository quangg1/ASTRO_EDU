# community

HTTP: `/api/forums`, `/api/posts`, `/api/comments`, `/api/news`, `/api/community`.

Public reads: `communityReadService`, `forumBootstrapService.ensureCohortForum`.

FE mirror: `client/src/features/community/public.ts`.
