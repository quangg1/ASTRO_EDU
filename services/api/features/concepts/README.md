# Concepts Feature

Ownership:

- Concept library (`Concept`)
- Taxonomy registry (`TaxonomyRegistry`)
- Concept/taxonomy APIs (`/api/concepts/*`)

Notes:

- Public API path remains `/api/concepts`.
- Scripts/bootstrap now import from this feature namespace.
