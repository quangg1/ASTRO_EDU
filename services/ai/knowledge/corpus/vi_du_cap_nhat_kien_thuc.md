# Ví dụ feed kiến thức mới

Đặt các file `.md` trong thư mục này rồi chạy `python scripts/build_rag_index.py` hoặc gọi `POST /knowledge/rebuild` trên service AI.

James Webb Space Telescope đã ghi nhận nhiều thiên hà ở đỏ dịch chuyển cao, giúp hiểu rõ hơn về thiên hà sơ khai chỉ vài trăm triệu năm sau Vụ Nổ Lớn.

Các phát hiện về exoplanet trong vùng cư trú được của sao M-type cho thấy tiềm năng môi trường lỏng trên các hành tinh đất đá quay gần sao lùn đỏ.
