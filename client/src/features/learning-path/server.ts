/** Server-only learning-path fetchers for RSC pages. Client code: features/learning-path/public. */
import 'server-only'

export { getMergedLearningModules, getMergedLearningPathData } from './api/server'
